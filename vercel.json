{
  "version": 2,
  "buildCommand": "cd server && npm install --no-audit --no-fund && npx prisma generate && cd ../client && npm install --no-audit --no-fund && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
